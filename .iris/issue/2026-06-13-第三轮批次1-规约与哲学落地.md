---
title: 第三轮批次1：规约与哲学落地
status: Done
labels: [round-3]
---
需求来源：`issue/2026-06-12-第三轮需要处理的问题.md` 的 G 条（设计哲学修订）与 H 条（规约跟随迭代）。

范围：

- software-definition.md：哲学修订（坚持不内嵌 agent / 不做订阅，放行用户 BYOK 辅助功能）+ "裸启动"措辞修订（不传任务 prompt，可经用户配置注入上下文）。
- `src/main/iris-templates.ts`：CONSTITUTION_TEMPLATE 改六态状态机（存盘值即显示值），report 两态 + "正文 append-only、frontmatter 状态流转允许"。
- 同步本项目 `.iris/CONVENTIONS.md`（用户已明确授权，仅限本轮拍板范围）。

## 实施记录

2026-06-13 完成，三处改动：

1. **software-definition.md**：
   - §2「哑壳 + 智能外包」追加一句：坚持不做 agent / 不做订阅，不拒绝用户 BYOK 驱动的辅助功能——"壳不自带智能，不等于拒绝用户自带的智能"。
   - §4 核心手势："裸启动（不传任何 prompt）"改为"不传任务 prompt；可经用户自己的 agent 配置注入上下文"，并新增一条 bullet 说明注入是渲染层适配不是协议（hook / flag / 降级三层，壳依然只设 `FOCUS_DOC`）。
   - §7「当前不做什么」第一条补 BYOK 例外说明。
2. **iris-templates.ts CONSTITUTION_TEMPLATE**：
   - 规则 6 改为六态（Todo → In Progress → In Review → Done，Blocked / Canceled 为旁路态），明确"存盘值即显示值，按显示原样书写"；report 两态 Active / Backlog。
   - `report/` 语义改为"正文 append-only，frontmatter 状态流转允许"。
3. **`.iris/CONVENTIONS.md` 全量同步到新模板**（用户已授权）。注意：同步顺带带入了第二轮已拍板但当时未落盘的"No unsolicited files"规则（替换旧规则 4 "Session journal"，该条第二轮已明确废除）——属于既有拍板的补落地，不是本轮新增裁决。

### 决策点

- `protocol:` 版本号保持 1 不升：v1 尚未发布，处于迭代期，模板即事实规范；升版会触发"旧版提示 diff"机制（该机制本身也尚未实现），留待首个正式发布再考虑。

### 第一轮验收反馈（2026-06-13）

用户在清单批注：规约规则 6 应补一条——**AI 未经明确要求不应把状态改为 Done / Canceled（会让 issue 从左栏活动透镜消失）**。

已落（规则 6 追加）："Never resolve an issue unprompted." 解决性流转（issue → Done/Canceled、report → Backlog）会移出活动透镜，是用户的决定；agent 可自行推进到 In Review，但收尾等用户开口。模板（iris-templates.ts）与盘上 `.iris/CONVENTIONS.md` 同步。验收通过即转 Done。
