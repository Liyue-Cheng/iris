---
title: 左栏上方图标治理
status: Done
---
新增 打开最近的项目（后端相关记录应该已经实现）

取消 文件数

重新排序

## 2026-08-06 实现记录

* 左栏顶部新增“打开最近的项目”菜单，直接复用现有最近项目查询与项目打开能力；菜单每次展开时刷新，失效路径保留展示但不可点击。

* 顶部图标重排为：打开项目、最近项目、新窗口打开、新建工作区、排序、筛选、待办、Git。

* 将“文件数”按同音笔误“文件树”处理，移除裸文件树图标及左栏裸树渲染分支，左栏统一显示透镜树。

* 修复相邻图标间快速移动时 Tooltip 滞留或跳换：所有提示保持 400ms 延迟，关闭跳过延迟窗口，并禁止提示浮层接管鼠标悬浮。

* `npm run typecheck` 在 UI 改动完成后通过；完整 Vitest 共 8 个测试文件、76 个测试通过。

* 后续复查时，工作区并行新增的 `src/renderer/lib/terminal-dims.test.ts` 缺少 `workspacePath` 参数，导致全量类型检查失败；该文件不属于本 issue，未改动。

- [x] 在运行中的应用里快速横向划过左栏图标，确认提示不会滞留或无延迟跳换，并验收最近项目菜单与图标顺序。

