---
title: md代提示框显示不合理编辑
status: Done
---
行号效果突兀，框和内容间距过来，语言信息不应该悬浮才显示  重新设计

## 修改内容

在 `src/renderer/styles/global.css` 代码块覆写区做了三项调整：

- [x] 隐藏行号（`.cm-lineNumbers { display: none }`）
- [x] 收紧间距（`padding: 4px 0 8px` 覆盖 Crepe 默认的 `8px 20px 20px`）
- [x] 语言标签始终可见（`opacity: 1`），非 hover 态用 `var(--rp-muted)` 柔和显示，hover 变亮

## 复查修正

首次实现仅间距调整生效。Crepe 7.21.2 的行号由 CodeMirror `basicSetup` 注入，且语言按钮规则与应用覆写的特异性相同、后加载的 Crepe 样式会重新将其隐藏。

- [x] 改为隐藏代码块完整的 `.cm-gutters`，同时移除行号及其残留占位
- [x] 按真实 DOM 补全 `.tools .language-button` 选择器，提高特异性，保证非 hover 状态维持 `opacity: 1`

## 间距与复制按钮调整

- [x] 保持上下间距不变，将代码块左右内边距从 `0` 调整为 `12px`
- [x] 右侧复制操作改为 `28px` 固定尺寸的常显复制图标，隐藏视觉文字并保留“复制”无障碍名称

## 当前行高亮修正

- [x] CodeMirror 编辑区未获得焦点时清除 `.cm-activeLine` 背景，避免初始化光标导致第一行持续高亮
- [x] 光标进入 CodeMirror 代码内容时保留当前行提示，操作语言或复制按钮不会触发行高亮

## 控件视觉与弹层修正

- [x] 代码块圆角从 `8px` 缩小到 `4px`
- [x] 语言入口移除底色、圆角和下拉箭头，改为独立文字标签
- [x] 代码块改为允许溢出，语言选择弹层不再被代码块边界截断
- [x] 复制操作移除圆形底板和胶囊圆角，仅显示复制图标

## 语言标签对齐

- [x] 语言标签字重提高到 `600`，与代码正文形成层级区别
- [x] 标签左侧补入 `6px` 内边距，与 CodeMirror `.cm-line` 的正文起始位置对齐
