---
title: 构建与测试现状
reflects: a1b3c2d
---
# 构建与测试现状

* 构建：`npm run build`，产物在 `dist/`

* 单测：`npm test`（vitest），覆盖率约 70%

* CI：GitHub Actions，main 分支必须绿

> 注意：本文档没有 `status:` 字段——status 类型的文档不是任务，
> 不强制携带任务字段。这是合法的。发动  大撒大撒 非

