---
title: 新建 issue：左栏排序不稳定
---

# 新建 issue：左栏排序不稳定

应用户要求新建一条 issue 用于验证写入路径：
`issue/2026-06-11-sidebar-sort-order.md`（status: todo）。

`$FOCUS_DOC` 未设置，按约定写入根工作区的 `issue/` 文件夹，
命名采用 `YYYY-MM-DD-<slug>.md`。本夹具同时是 iris-scanner 的
单测数据，已跑通 12 个单测确认新增文件未破坏扫描断言。
